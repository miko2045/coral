{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npx terser dist/static/app.js -o dist/static/app.js --compress drop_console=true,passes=2 --mangle toplevel --module",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.8",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "terser": "^5.46.1",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
